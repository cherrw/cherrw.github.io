{
	"name": "scsblog",
	"version": "1.0.0",
	"description": "A blog site for scs from szsl.",
	"main": "index.js",
	"scripts": {
		"dev": "vitepress dev docs --port 35173",
		"build": "vitepress build docs",
		"preview": "vitepress preview docs",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"vitepress": "1.0.0-alpha.35",
		"vue": "^3.2.45"
	},
	"pnpm": {
		"peerDependencyRules": {
			"ignoreMissing": [
				"@algolia/client-search"
			]
		}
	}
}
